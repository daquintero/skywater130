
# Please read the environment setup instructions provided in docs/openlane_integration before attempting this notebook, otherwise it will not work.

# ## Default Example Setup

# We use in our example the standard `spm` design which is the default example given by `OpenLane` as described in [this tutorial]() but we will build it using the Python API and then interact with it through GDSFactory.

